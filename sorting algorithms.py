# Bubble sort

def bubble_sort(num_arr):
    for i in range(0, len(num_arr)+1):
        for j in range(0, len(num_arr) ):
            if num_arr[i] > num_arr[j]
            arr_num[i], arr_num[j] = arr_num[j], arr_num[i]
    return arr_num



# Driver code
arr_num = [1,2,4,6,2,8,5,7]
print(arr_num)
